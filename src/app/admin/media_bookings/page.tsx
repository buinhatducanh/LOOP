/**
 * Admin Media Bookings Page — LOOP Solutions
 * Route: /admin/media_bookings
 *
 * Re-exports the existing MediaBookingsPage client component from
 * /admin/media/page.tsx — same functionality, accessible via both URLs.
 * The sidebar links to /admin/media_bookings for clarity.
 */

export { default } from "../media/page";