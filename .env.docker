# PostgreSQL (docker-compose local)
DATABASE_URL="postgresql://loop:loop_dev_password@localhost:5432/loopdb?sslmode=disable"

# Disable Upstash in local dev (use Docker Redis instead)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
